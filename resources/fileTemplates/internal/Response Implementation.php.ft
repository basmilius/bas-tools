<?php
declare(strict_types=1);
#if (${NAMESPACE})

namespace ${NAMESPACE};
#end

use Columba\Router\Context;
use Columba\Router\Response\AbstractResponse;

#parse("PHP Class Doc Comment.php")
final class ${NAME} extends AbstractResponse
{

	/**
	 * {@inheritdoc}
	 */
	protected abstract function respond(Context ${DS}context, ${DS}value): string
	{
		// TODO: Return ${DS}data to the browser somhow.
	}

}
